## X (Twitter) API
# OAuth 2.0 credentials (for API v2, metrics, etc.)
# Get these from https://developer.x.com

# Your app's Client ID (OAuth 2.0)
X_CLIENT_ID=your_client_id_here

# Your app's Client Secret (OAuth 2.0)
X_CLIENT_SECRET=your_client_secret_here

# Redirect URI - must match what's configured in X Developer Portal
# For local dev: http://localhost:3000/api/x/callback
# For production: https://yourdomain.com/api/x/callback
X_REDIRECT_URI=http://localhost:3000/api/x/callback

# OAuth 1.0a credentials (for media upload and user-context actions)
# These are the app-level consumer key/secret for your X app
X_OAUTH1_CONSUMER_KEY=your_oauth1_consumer_key_here
X_OAUTH1_CONSUMER_SECRET=your_oauth1_consumer_secret_here

